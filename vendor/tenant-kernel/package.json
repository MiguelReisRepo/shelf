{
  "name": "@mr/tenant-kernel",
  "version": "0.1.0",
  "private": true,
  "description": "Contrato único de branding/tenant para o marketplace de apps white-label (barbershop, KidsFashionClub, marketplace). Só tipos + helpers puros — zero framework.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
